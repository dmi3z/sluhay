export interface SupportPerson {
  name: string;
  avatar: string;
  since: string;
  cost: string;
  role: string;
}

export interface SupportCompany {
  name: string;
  role: string;
  logo: string;
  cost: number;
  since: string;
}
